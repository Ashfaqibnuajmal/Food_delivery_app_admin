import 'package:flutter/material.dart';

class FooditemScreen extends StatelessWidget {
  const FooditemScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        body: Center(
      child: Text("food item screens"),
    ));
  }
}
